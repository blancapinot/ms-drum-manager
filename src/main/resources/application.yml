
spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/drummanager
    username: postgres
    password: pedro2020
  sql:
    init:
      mode: always
server:
  servlet:
    context-path: /api